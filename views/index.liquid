{% render 'partials/head.liquid', title: 'Squad page' %}

<form action="/search" method="GET"> 
    <input type="search" name="q" placeholder="Zoek studenten op naam"> 
    <button type="submit">Zoek</button> 
</form>

<ul>
  <li><a href="/">A-Z</a></li>
  {% for letter in alfabetArray %}
    <li><a href="/a-z/{{ letter }}" class="alfabet">{{ letter | upcase}}</a></li>
  {% endfor %}
</ul>

{% if isSearch %}

  <h2>Je hebt gezocht op: {{ query }}</h2>  

{% else %}

  <h2>Alle studenten met {{ letter }}</h2>

{% endif %}

{% if persons.size > 2 %}

  <p>{{ persons.size }} personen die hier aan voldoen.</p>
  
{% elsif persons.size > 0 %}
    
  <p>{{ persons.size }} persoon die hier aan voldoet.</p>

{% endif %}

<ul>
  {% for person in persons %}
   
    <li><a href="/student/{{ person.id }}" style="--color-favorite: {{ person.fav_color }}" class="person-list-item">

    {% if person.vibe_emoji %}
    
      {{ person.vibe_emoji }} 
    
    {% else %}

    💩

    {% endif %}

    
     {{ person.name }} 
    
    {% if person.vibe_emoji %}
    
      {{ person.vibe_emoji }} 
    
    {% else %}

    💩

    {% endif %}

    {% if person.mugshot %}

      <img src="https://fdnd.directus.app/assets/{{ person.mugshot }}?width=400" alt="Profile picture">

    {% else %}

      <img src="https://cdn.pixabay.com/photo/2023/02/18/11/00/icon-7797704_640.png" alt="Profile picture">

    {% endif %}

    </a></li>
    
  {% endfor %}

  {% if persons.length == 0 %}
    
    <li class="error-message">😢 Er zijn geen studenten met deze letter 😢</li>

  {% endif %}
</ul>
</main>


<details>
  <summary>open</summary>
  {{ persons | json }}
</details>

{% render 'partials/foot.liquid', squads: squads %}